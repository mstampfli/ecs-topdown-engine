#include "Camera.h"

/*Camera::Camera(float px, float py, float pz, float yaw = -90.0f, float pitch = 0.0f)
     : yaw(yaw), pitch(pitch), fov(45.0f), aspectRatio(16.0f / 9.0f), nearPlane(0.1f), farPlane(100.0f) { //Define Values
    //Aspect ration should be screen width / height (4 / 3)
    position[0] = px; position[1] = py; position[2] = pz;
    up[0] = 0.0f; up[1] = 1.0f; up[2] = 0.0f;
    updateVectors();
}

Camera::~Camera() {
    delete[] position;
    delete[] up;
}
void Camera::updateVectors(){

}*/